import StacksBackend from "@/sections/stacks/stacks-backend";
import StacksIntro from "@/sections/stacks/stacks-intro";

export default function StacksPage() {
  return (
    <main>
      <StacksIntro />
      <StacksBackend />
    </main>
  );
}
