export interface SectionLayoutProps{
    id: string;
    className?: string;
    children: React.ReactNode;
}

export interface ContainerLayoutProps{
    className?: string;
    children: React.ReactNode;
}